import React from 'react';
import ItemCarousel from '../Carousel/ItemCarousel';

const ProductList = () => {
  return (
    <div>
      <ItemCarousel />
    </div>
  );
};

export default ProductList;
